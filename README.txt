This program will print out a calendar for a given year.

When run, the program will prompt the user to input a year. After the user types in a year and presses enter, a calendar following the Gregorian calendar will be printed.